{{#each ideas}}
	<div class="card card-body mb-2">
		<h4>{{title}}</h4>
		<p>{{details}}</p>
		<a href="/ideas/edit/{{id}}" class="btn btn-secondary btn-block"><i class="fas fa-pencil-alt"></i> Edit</a>
	</div>
{{else}}
		<p>No videos ideas listed</p>
{{/each}}
